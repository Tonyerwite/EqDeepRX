# Public EqDeepRx Code Search

GitHub was rechecked on 2026-09-10 with the authenticated `Tonyerwite` account:

```text
gh search repos EqDeepRx
-> Tonyerwite/EqDeepRX only

gh search code "DenoiseNN DetectorNN EqDeepRx"
-> no results
```

The destination repository is [github.com/Tonyerwite/EqDeepRX](https://github.com/Tonyerwite/EqDeepRX). No author or third-party public implementation of the DenoiseNN + parallel RZF/LMMSE + shared DetectorNN/DemapperNN pipeline was found, so there was no EqDeepRx source tree to reuse. The result is an independent implementation from the paper, with the supplied DeepRx project used as the local integration reference.

Absence from GitHub search does not prove that private or unindexed source code does not exist.
